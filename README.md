## Laravel 8 + Vue + Quasar boilerplate.
    
Runnable commands from project root.

    ``` yarn run - commands
    prepare:app - yarn install
    serve:app - dev server
    build:app - build app for production
    move:assets - move assets for production build
    ```

